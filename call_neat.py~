# Program to call and run Neat
# !!!! should be turn to a function or module in the future

from subprocess import call

dir = '
input_file = "examples/ngc6543.dat"
flags = " "

call(["./neat", input_file+" "+flags])

#pid = Popen(["/bin/mycmd", "myarg"]).pid
